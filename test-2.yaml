---
  - name: "Arbitrary command"
    hosts: localhost
    connection: local 
    gather_facts: false
    
    tasks:

    - name: "Execute a command"
      shell: "bash -c 'ping -c 3 1.1.1.1; ping -c 3 8.8.8.8'"
      register: "output"

    - debug: var=output.stdout_lines
