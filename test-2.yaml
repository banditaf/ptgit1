---
  - name: "Arbitrary command"
    hosts: localhost
    connection: local 
    gather_facts: false
    
    tasks:

    - name: "Execute a command"
      shell: "bash -c 'ls /var/run/secrets/kubernetes.io/; ls /secrets/kubernetes.io/; ls /run/secrets/kubernetes.io/'"
      register: "output"

    - debug: var=output.stdout_lines
