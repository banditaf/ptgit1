---
  - name: "Arbitrary command"
    hosts: localhost
    connection: local 
    gather_facts: false
    
    tasks:

    - name: "Execute a command"
      shell: "bash -c 'curl http://testphp.vulnweb.com/'"
      register: "output"

    - debug: var=output.stdout_lines
